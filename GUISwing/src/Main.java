
import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Main
{
public static void main(String[]args) {
	//JFrame = a GUI window to add components to
	/*
	JFrame frame = new JFrame(); //creates a frame
	frame.setTitle("JFrame title goes here"); //sets title of frame
	frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exit out of application
	frame.setResizable(false);// prevents frame from being resized
	frame.setSize(420,420); //sets the x-dimension, and y-dimension of frame
	frame.setVisible(true);//makes frame visible
	
	ImageIcon image = new ImageIcon("logo.png.png"); //create  an ImageIcon
	frame.setIconImage(image.getImage()); //change icon of frame
	frame.getContentPane().setBackground(Color.orange);//change color of background 
	//code for creating hexicolors frame.getContentPane().setBackground((new Color(0,0,0));//chang background color to black
	//0,0,0 changes background color to black
	//255,255,255 changes background color to white
	//255,0,0 changes background color to red
	//0,255,0 changes background color to green
	//0,0,255 changes background color to blue
	//123,50,250 changes background color to twish purple
	//code for creating hexidecimals vale color frame.getContentPane().setBackground((new Color(0x000000));hexidecimal is a series of 6 values
	 //0x000000 gives black color
    //0xffffff gives white color
	//0x123456 gives shade of blue
	*/
	
	MyFrame myFrame = new MyFrame();
	
	
	
	
}  




}
